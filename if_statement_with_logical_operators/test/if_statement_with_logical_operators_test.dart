import 'package:if_statement_with_logical_operators/if_statement_with_logical_operators.dart';
import 'package:test/test.dart';

void main() {
  test('calculate', () {
    expect(calculate(), 42);
  });
}
